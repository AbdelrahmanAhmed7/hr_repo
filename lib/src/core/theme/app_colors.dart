import 'package:flutter/material.dart';

class AppColors {
  // Brand Colors (Deep Blue & Bright Blue)
  static const Color primary = Color(0xFF1E3A8A);      // Deep Blue
  static const Color primaryDark = Color(0xFF172554);  // Extra Dark Blue
  static const Color primaryLight = Color(0xFF3B82F6); // Bright Blue (Secondary)
  static const Color primaryTint = Color(0xFFEFF6FF);  // Very light blue

  // Neutrals (Slate)
  static const Color textPrimary = Color(0xFF1E293B);  // Dark Slate
  static const Color textSecondary = Color(0xFF64748B); // Slate Gray
  static const Color textTertiary = Color(0xFF94A3B8);
  static const Color border = Color(0xFFE2E8F0);
  static const Color background = Color(0xFFF8FAFC);   // Off-white background
  static const Color backgroundSecondary = Color(0xFFF1F5F9); 
  static const Color surface = Color(0xFFFFFFFF);      // Pure White

  // Semantic
  static const Color success = Color(0xFF10B981);      // Emerald
  static const Color successTint = Color(0xFFD1FAE5);
  static const Color error = Color(0xFFEF4444);        // Rose Red
  static const Color errorTint = Color(0xFFFEE2E2);
  static const Color warning = Color(0xFFF59E0B);      // Amber
  static const Color info = Color(0xFF3B82F6);         // Bright Blue matching secondary
}
